# Модуль: <название>

> Шаблон спеки модуля. Копируй при добавлении нового модуля.
> Структура повторяет «шесть типов контекста» из методологии (instructions, knowledge,
> memory, examples, tools, guardrails).

## 1. Назначение
Одно-два предложения: за что отвечает модуль и за что НЕ отвечает.

## 2. Этап дорожной карты
M? — когда появляется/дозревает.

## 3. Публичный интерфейс
Функции/классы, которые модуль отдаёт наружу (сигнатуры, кратко).

```python
# пример
def do_something(x: X) -> Y: ...
```

## 4. Входы / Выходы
- **Вход:** откуда и что приходит.
- **Выход:** что и куда отдаёт.

## 5. Зависимости
- **Внутренние:** другие модули `app/*`.
- **Внешние:** библиотеки, API, сервисы.

## 6. Данные
Какие сущности из `docs/architecture/data-model.md` читает/пишет.

## 7. Guardrails / ограничения
Что модулю запрещено; лимиты; валидации.

## 8. Тесты / evals (контракт)
Что проверяем; критерии «правильно».

## 9. Открытые вопросы
Нерешённые развилки.

## 10. Статус
`не начат` | `в работе` | `готов`
