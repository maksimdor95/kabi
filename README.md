# Kabi — персональный AI-менеджер карьеры

Персональный AI-агент в Telegram, который знает о пользователе всё релевантное для
карьеры и **проактивно** приносит подходящие вакансии и приглашения на выступления —
как персональный менеджер у звезды.

## Идея в одном абзаце

Обычные джоб-сайты пассивны: ищешь и фильтруешь ты сам. Kabi работает наоборот — как
менеджер звезды: сам находит возможности, отсеивает шум и приносит только «твоё»,
объясняя «почему это про тебя».

## Статус

**Сделано:** M1–M6b + **M8** (evals подборки и «почему ты»).

**Сейчас следующий:** **M9** — диалог-советник.
M7 (TG + career API-волна A + Getmatch + Habr) и M8 (evals) закрыты; пул `jobs_m7_v2`.
Карта источников: [`docs/services/ingestion.md`](docs/services/ingestion.md) §9.

**Потом:** M10 Alembic → M11 Mini App.

> Номера этапов исторические: M8 закрыли раньше M7 — так и оставляем
> (контракт качества до расширения воронки). В таблице ниже — статусы, не очередь.

## Дорожная карта

| Этап | Содержание | Статус |
|------|------------|--------|
| **M1** | Бот + парсинг CV + профиль + онбординг + enrichment | ✅ |
| **M2** | HH + SuperJob + мэтчинг + ежедневная подборка | ✅ |
| **M3** | Talks seed (питч) + `/talks` + напоминания о дедлайнах | ✅ |
| **M4** | Фидбек → embedding blend + черновики откликов | ✅ |
| **M5** | Монитор известных CFP-URL (`open_cfp.yaml`) | ✅ |
| **M6** | Discovery CFP по региону/нише | ✅ |
| **M6b** | Настраиваемое расписание рассылок | ✅ |
| **M7** | Источники: HH, SJ, TG, Habr, Getmatch + career (Яндекс/Сбер/Альфа/WB/МТС API, Авито/VK/Т-Банк HTML) | ✅ |
| **M8** | Evals: подборка + «почему ты» — [`docs/services/evals.md`](docs/services/evals.md) | ✅ |
| **M9** | Диалог «как менеджер» после онбординга (не болталка, а советник) | — |
| **M10** | Alembic вместо `create_all` + ручных `ALTER` | — |
| **M11** | Telegram Mini App (богатый UI на FastAPI) | — |

### Что значит оставшееся (коротко)

| | Зачем |
|--|--------|
| **M9** | Свободный чат уже есть (LLM), но слабый: нужен менеджерский тон, память диалога, инструменты (профиль/подборка), без to-do на день. |
| **M10** | Схема БД растёт — без миграций опасно деплоить; до Mini App лучше навести порядок. |
| **M11** | Карточки и профиль удобнее в UI, чем в Telegram-сообщениях; только после стабильного ядра. |

**Гипотеза M6:** готового API/маркетплейса открытых CFP под product/RU нет —
значит, умение **сам находить и копить** CFP по карте и нише — рыночный актив,
а не «дописать yaml».

**Вне scope (явно отложено):** мультиюзер, биллинг, success fee, сбор данных о третьих лицах.

## Структура репозитория

```
Kabi/
├── AGENTS.md              # правила/харнесс проекта (читать первым)
├── SPEC.md                # продуктовая спецификация MVP
├── docs/
│   ├── architecture/      # overview, ADR, модель данных
│   └── services/          # по одному MD на каждый модуль
├── bot/                   # Telegram-интерфейс (тонкий слой)
├── app/                   # ядро: агент, домен, инфраструктура
└── tests/                 # тесты и evals
```

## Как ориентироваться

1. Прочитай `AGENTS.md` — правила и hard rules.
2. `docs/architecture/overview.md` — карта системы и границы модулей.
3. `docs/services/<module>.md` — спека конкретного модуля.

## Локальный запуск (заготовка)

```bash
cp .env.example .env   # заполнить токены
docker compose up -d   # postgres + redis
# запуск бота
PYTHONPATH=. python -m bot.main
```
